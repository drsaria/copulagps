% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ate.R
\name{apply_augmented}
\alias{apply_augmented}
\title{Apply a user-specified model function to the augmented dataset}
\usage{
apply_augmented(gps, FUN, outcome = NULL, ...)
}
\arguments{
\item{gps}{Object of class \code{"gps_weights"}}

\item{FUN}{A function \code{f(data, ...)} applied to the augmented data frame.
The data frame will have columns \code{T1}, \code{T2}, \code{w}, and optionally
\code{outcome}.  Rows with \code{w = NA} are hull-trimmed and should typically be
excluded.  See examples.}

\item{outcome}{Optional numeric outcome vector (length n) to attach.}

\item{...}{Additional arguments forwarded to \code{FUN}.}
}
\value{
Whatever \code{FUN} returns.
}
\description{
Constructs the augmented data frame via \code{\link[=get_augmented_data]{get_augmented_data()}} and passes
it to \code{FUN}.  The function must accept a \code{data.frame} as its first argument.
GPS weights are available as column \code{"w"} and hull-trimmed observations have
\code{w = NA}.
}
\examples{
\dontrun{
# Unweighted linear model on augmented data
apply_augmented(gps, outcome = y_obs,
  FUN = function(d) lm(outcome ~ T1 + T2, data = d, weights = d$w))

# User glm on non-trimmed rows only
apply_augmented(gps, outcome = y_obs,
  FUN = function(d) {
    d <- d[!is.na(d$w), ]
    glm(outcome ~ T1 * T2, data = d, weights = d$w, family = Gamma(link = "log"))
  })
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ate.R
\name{estimate_ate}
\alias{estimate_ate}
\title{Estimate average treatment effects using GPS weights}
\usage{
estimate_ate(
  gps,
  outcome,
  treatment_levels,
  model_type = "auto",
  family = stats::gaussian(),
  covariates = NULL,
  robust_se = TRUE,
  estimand = c("ATE", "OR"),
  censoring_method = c("standard", "uncens", "substitution"),
  sub_values = "lod_sqrt2"
)
}
\arguments{
\item{gps}{Object of class \code{"gps_weights"}}

\item{outcome}{Numeric outcome vector of length n}

\item{treatment_levels}{Matrix of treatment regimes to compare (each row is
one regime; consecutive pairs are contrasted).  If omitted, defaults to
the 75th vs 25th percentile of each treatment.}

\item{model_type}{\code{"auto"}, \code{"lm"}, \code{"glm"}, \code{"gam"}, or \code{"bart"}.
\code{"auto"} (default) detects binary outcomes and uses logistic regression.
\code{"gam"} fits a GAM with smooth terms via \code{mgcv} (requires mgcv).
\code{"bart"} fits an unweighted BART model and uses GPS augmentation for ATE
(requires dbarts); GPS weights enter via AIPW correction, not direct weighting.}

\item{family}{GLM family when \code{model_type = "glm"}.  Default: \code{gaussian()}.}

\item{covariates}{Optional n × p covariate matrix added to the outcome model.}

\item{robust_se}{Use HC1 heteroscedasticity-robust standard errors.
Default: \code{TRUE} (not applicable to BART).}

\item{estimand}{\code{"ATE"} (default) or \code{"OR"}.  When \code{"OR"}, fits a
GPS-weighted logistic regression \emph{without covariates} and returns the
marginal structural odds ratio at the specified contrast, following
Brown et al. (2021).  Requires a binary outcome.}

\item{sub_values}{Substitution values used when \code{censoring_method = "substitution"}.  Either \code{"lod_sqrt2"} (default, LOD / sqrt(2)),
\code{"lod"} (the LOD value itself), or a numeric vector of length d
supplying one substitution value per treatment.}
}
\value{
Object of class \code{"gps_ate"}
}
\description{
Fits a weighted outcome regression and computes ATE contrasts between
consecutive rows of \code{treatment_levels} via the delta method.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/balance.R
\name{.compute_fstats}
\alias{.compute_fstats}
\title{Weighted F-statistics for T_j ~ X per treatment (internal)}
\usage{
.compute_fstats(T_mat, X_mat, w_valid)
}
\arguments{
\item{T_mat}{n × d numeric treatment matrix.}

\item{X_mat}{n × p numeric covariate matrix (factors already expanded).}

\item{w_valid}{n-length weight vector; NAs treated as 0.}
}
\value{
data.frame with one row per treatment: \code{treatment}, \code{f_pre},
\code{f_post}, \code{p_pre}, \code{p_post}, \code{df1}, \code{df2}.
}
\description{
Fits \code{lm(T_j ~ ., data = X_df, weights = w)} for each treatment column and
returns the F-statistic before (unweighted) and after (GPS-weighted) testing
H0: T_j is linearly uncorrelated with all covariates.  Under perfect balance
the weighted F converges to 1 in expectation regardless of n and p.
}
\details{
Categorical covariates must already be expanded to dummy columns before
calling this function (as done by \code{assess_balance()}).
}
\keyword{internal}

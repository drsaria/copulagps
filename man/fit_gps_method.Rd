% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{fit_gps_method}
\alias{fit_gps_method}
\title{Estimate GPS weights using a specified method}
\usage{
fit_gps_method(
  data,
  margins = NULL,
  outcome = NULL,
  method = "copulagps",
  user_methods = NULL,
  treatment_levels,
  ...
)
}
\arguments{
\item{data}{Prepared data list from \code{\link[=prepare_data]{prepare_data()}}.}

\item{margins}{Character vector of length d (required for \code{"copulagps"}).}

\item{outcome}{Optional numeric outcome vector.  Required for \code{"aipw"} and
to return ATE estimates for any method.}

\item{method}{Character scalar or function: \code{"copulagps"}, \code{"aipw"}, or
the name of a method registered in \code{user_methods}.  Alternatively, supply
a function directly — it will be called as \code{method(data, ...)}.}

\item{user_methods}{Named list of up to 2 user-defined method functions.
Each function must accept \verb{(data, outcome, ...)} and return a list with
at minimum \verb{$weights} (numeric vector, length n).  The name becomes the
method label.}

\item{treatment_levels}{Treatment regime matrix for ATE contrasts (passed to
\code{\link[=estimate_ate]{estimate_ate()}}).}

\item{...}{Additional arguments forwarded to \code{\link[=estimate_gps]{estimate_gps()}} for the
\code{"copulagps"} method, or to the user method function.}
}
\value{
Object of class \code{"gps_method_result"} with components:
\describe{
\item{\code{method}}{Method name used}
\item{\code{gps}}{\code{gps_weights} object (or equivalent list for custom methods)}
\item{\code{ate}}{\code{gps_ate} object, or \code{NULL} if no outcome supplied}
}
}
\description{
A unified interface for computing GPS weights and estimating ATEs under
multiple methods.  Built-in options are \code{"copulagps"} (the default copula
approach) and \code{"aipw"} (augmented IPW / doubly-robust estimator).  You can
also register up to two user-defined methods via \code{user_methods}.
}
\examples{
\dontrun{
d <- prepare_data(df, treatments = c("T1","T2"), covariates = c("age","sex"),
                  outcome = "Y")

# Built-in copulagps
res <- fit_gps_method(d, margins = c("normal","normal"),
                      outcome = d$outcome, method = "copulagps")

# Doubly-robust AIPW
res <- fit_gps_method(d, margins = c("normal","normal"),
                      outcome = d$outcome, method = "aipw")

# Custom method (e.g., CBGPS-style balancing)
my_method <- function(data, outcome, ...) {
  w <- rep(1 / nrow(data$y), nrow(data$y))  # uniform weights placeholder
  list(weights = w, gps = NULL)
}
res <- fit_gps_method(d, margins = c("normal","normal"),
                      outcome = d$outcome, method = "my_method",
                      user_methods = list(my_method = my_method))
}
}

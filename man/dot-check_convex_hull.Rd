% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/copula_utils.R
\name{.check_convex_hull}
\alias{.check_convex_hull}
\title{Identify observations inside the convex hull of the joint treatment distribution}
\usage{
.check_convex_hull(y)
}
\arguments{
\item{y}{n x 2 (or n x d) treatment matrix}
}
\value{
Logical vector of length n: \code{TRUE} = inside hull (keep),
\code{FALSE} = outside (candidate for trimming)
}
\description{
For d = 2, the convex hull is computed on the observed treatment values and
each observation is tested for membership.  Observations outside the hull
are in a region of the joint treatment space with no support in the data —
their GPS weights would extrapolate beyond any observed unit.
}
\details{
For d > 2 use \code{check_hull_overlap()} with \code{method = "mahalanobis"} instead;
convex hulls in high dimensions become geometrically loose and expensive to
compute.
}
\keyword{internal}

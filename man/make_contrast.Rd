% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/contrast.R
\name{make_contrast}
\alias{make_contrast}
\title{Build a treatment contrast matrix for ATE estimation}
\usage{
make_contrast(
  data,
  type = "quantile",
  vary = NULL,
  probs = c(0.75, 0.25),
  hold_at = "median"
)
}
\arguments{
\item{data}{Prepared data list from \code{\link[=prepare_data]{prepare_data()}} (must have \verb{$y}).}

\item{type}{One of:
\describe{
\item{\code{"quantile"}}{All treatments simultaneously at \code{probs[1]} vs
\code{probs[2]} quantile (default).  Returns a 2-row matrix.}
\item{\code{"single_vary"}}{Vary each treatment one at a time between
\code{probs[1]} and \code{probs[2]}; hold all others at \code{hold_at}.  Returns a
2d-row matrix with one high/low pair per treatment.}
}}

\item{vary}{Integer vector of treatment indices to vary when
\code{type = "single_vary"}.  Default: all treatments.}

\item{probs}{Length-2 numeric vector \code{c(high_prob, low_prob)}.
Default: \code{c(0.75, 0.25)}.}

\item{hold_at}{How to hold fixed treatments: \code{"median"} (default) or
\code{"mean"}.}
}
\value{
A numeric matrix with named rows and d columns (one per treatment).
Column names match treatment column names from \code{data$y} or \verb{T1, T2, ...}.
}
\description{
Constructs a matrix of treatment regimes to pass to \code{\link[=estimate_ate]{estimate_ate()}} as
\code{treatment_levels}.  Consecutive row pairs are contrasted.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/multi_chain.R
\name{combine_rubin}
\alias{combine_rubin}
\title{Combine M chain estimates using Rubin's rules}
\usage{
combine_rubin(chains, comparison = 1L)
}
\arguments{
\item{chains}{Object of class \code{"gps_chains"} from \code{\link[=run_chains]{run_chains()}}.}

\item{comparison}{Integer or character scalar selecting which ATE comparison
to pool when \code{mode = "ate"} and multiple contrasts exist.  Default: \code{1L}
(first contrast).}
}
\value{
Object of class \code{"rubin_result"} with components:
\describe{
\item{\code{estimate}}{Pooled point estimate(s)}
\item{\code{se}}{Total standard error(s)}
\item{\code{ci_lower}, \code{ci_upper}}{95\% CI based on Barnard-Rubin df}
\item{\code{within_var}}{Within-chain variance (Ū)}
\item{\code{between_var}}{Between-chain variance (B)}
\item{\code{total_var}}{Total variance (T = Ū + (1 + 1/M)B)}
\item{\code{fmi}}{Fraction of missing information}
\item{\code{df}}{Barnard-Rubin degrees of freedom}
\item{\code{M}}{Number of chains used}
}
}
\description{
Pools point estimates and standard errors across M chains following Rubin
(1987).  The total variance accounts for both within-chain variance (model
uncertainty) and between-chain variance (imputation uncertainty).
}
\details{
\strong{Supported inputs:}
\itemize{
\item \code{chains} with \code{mode = "ate"}: extracts ATE and SE from each
\code{"gps_ate"} object automatically.
\item \code{chains} with \code{mode = "user"}: expects each chain result to be a list
with named elements \verb{$estimate} (numeric scalar or vector) and \verb{$se}
(same shape).  Multiple named estimands are pooled independently.
\item \code{chains} with \code{mode = "gps"}: not directly combinable by Rubin's rules;
a warning is issued and GPS diagnostics are averaged instead.
}
}

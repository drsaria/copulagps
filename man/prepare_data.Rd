% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{prepare_data}
\alias{prepare_data}
\title{Prepare data for GPS estimation from a data frame}
\usage{
prepare_data(
  data,
  treatments,
  covariates,
  outcome = NULL,
  lod = NULL,
  formulas = NULL,
  scale_covariates = TRUE
)
}
\arguments{
\item{data}{A \code{data.frame} (or \code{tibble}) containing all variables.}

\item{treatments}{Character vector of column names for the treatment
variables.  Must identify exactly \code{d} columns (currently d = 2 for the
copula GPS estimator).}

\item{covariates}{Character vector of column names to use as covariates in
the treatment models.  All selected columns are included in every treatment
model unless per-treatment formulas are supplied via \code{formulas}.}

\item{outcome}{Optional character scalar naming the outcome column.  When
supplied the outcome is extracted and stored in \verb{$outcome}; it is \strong{not}
included in the treatment design matrices.}

\item{lod}{Optional numeric vector of limit-of-detection thresholds, one per
treatment (recycled to length \code{d}).  Observations with treatment values
below their respective LOD are flagged as left-censored.}

\item{formulas}{Optional named list of length \code{d} containing one-sided R
formulae (e.g. \code{~ age + sex}) specifying the covariates for each treatment
model individually.  Names should match \code{treatments}.  When \code{NULL} (the
default) the same set of \code{covariates} is used for all treatments, with an
intercept prepended.}

\item{scale_covariates}{Logical.  If \code{TRUE} (default), numeric covariates
are mean-centred and scaled to unit SD before constructing design matrices.
Factor/character columns are left as-is.}
}
\value{
A list with components:
\describe{
\item{\code{y}}{n × d numeric treatment matrix}
\item{\code{x}}{List of d design matrices (one per treatment), each n × p
with an intercept column}
\item{\code{outcome}}{Numeric vector of length n, or \code{NULL} if not supplied}
\item{\code{covariates}}{The covariate data frame (numeric columns possibly
scaled) used to build the design matrices}
\item{\code{lod}}{Numeric vector of length d, or \code{NULL}}
\item{\code{treatment_names}}{Character vector of treatment column names}
\item{\code{covariate_names}}{Character vector of covariate column names}
}
}
\description{
Converts a standard data frame into the internal list format expected by
\code{estimate_gps()}, validating inputs and constructing design matrices along
the way.
}
\examples{
set.seed(1)
df <- data.frame(
  T1 = rnorm(200, 2), T2 = rnorm(200, -1),
  age = rnorm(200, 50, 10), sex = rbinom(200, 1, 0.5),
  Y   = rnorm(200)
)
d <- prepare_data(df,
  treatments = c("T1", "T2"),
  covariates = c("age", "sex"),
  outcome    = "Y"
)
str(d)
}

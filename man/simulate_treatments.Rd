% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate.R
\name{simulate_treatments}
\alias{simulate_treatments}
\title{Simulate bivariate treatment data with a known copula structure}
\usage{
simulate_treatments(
  n,
  copula_family = 3L,
  copula_par = 2,
  marginal_params = list(list(mean = 2, sd = 1), list(mean = -1, sd = 1.5)),
  covariates = NULL,
  treatment_effects = NULL,
  lod = NULL
)
}
\arguments{
\item{n}{Sample size}

\item{copula_family}{Copula family code: 1 = Gaussian, 3 = Clayton,
4 = Gumbel, 5 = Frank.  Default: \code{3} (Clayton).}

\item{copula_par}{Copula dependence parameter.  Default: \code{2}.}

\item{marginal_params}{List of 2 sublists, each with \verb{$mean} and \verb{$sd}
(scalar).  Default: \code{list(list(mean=2, sd=1), list(mean=-1, sd=1.5))}.}

\item{covariates}{Optional n × p covariate matrix.}

\item{treatment_effects}{Optional list of 2 numeric vectors (length p each)
giving covariate-to-treatment effects on the mean.}

\item{lod}{Optional numeric vector of length 2 for left-censoring thresholds.
When supplied, treatment values below the LOD are set to the LOD.}
}
\value{
List with \verb{$y} (n × 2 treatment matrix), \verb{$x} (list of design
matrices), \verb{$u_true}, \verb{$covariates}, \verb{$true_copula}, \verb{$marginal_params},
and optionally \verb{$lod}.
}
\description{
Generates n observations from a bivariate copula with Normal marginals,
optionally with covariate effects on treatment means.
}

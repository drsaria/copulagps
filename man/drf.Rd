% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/drf.R
\name{drf}
\alias{drf}
\title{Estimate the dose-response function from a fitted ATE object}
\usage{
drf(
  ate_obj,
  grid = NULL,
  vary = c("marginal", "joint"),
  n_grid = 50L,
  alpha = 0.05
)
}
\arguments{
\item{ate_obj}{Object of class \code{"gps_ate"} returned by \code{\link[=estimate_ate]{estimate_ate()}}.
Must have been fit with \code{model_type} in \code{c("lm", "glm", "gam")}.
BART objects are not supported (no closed-form prediction at new treatment
values).}

\item{grid}{Optional.  For \code{vary = "marginal"}: a named list with one
numeric vector per treatment, e.g.
\code{list(T1 = seq(0, 3, length.out = 50))}.  For \code{vary = "joint"} (d = 2
only): a named list with vectors for both treatments.  If \code{NULL} (default)
the grid spans the 5th–95th percentile of each observed treatment.}

\item{vary}{\code{"marginal"} (default) or \code{"joint"}.
\describe{
\item{\code{"marginal"}}{Vary each treatment one at a time, holding all
other treatments at their GPS-weighted median.  Returns one data
frame per treatment.}
\item{\code{"joint"}}{Vary all treatments simultaneously over a 2D grid
(d = 2 only).  Returns a single data frame with columns
\code{T1}, \code{T2}, \code{mu_hat}.  Standard errors are not computed for the
joint grid (use marginal slices for inference).}
}}

\item{n_grid}{Number of grid points per treatment when \code{grid = NULL}.
Default: 50.}

\item{alpha}{Significance level for confidence intervals.  Default: 0.05.}
}
\value{
Object of class \code{"gps_drf"}, a list with elements:
\describe{
\item{\code{slices}}{For \code{vary = "marginal"}: a named list of data frames,
one per treatment, each with columns \code{treatment}, \code{t}, \code{mu_hat},
\code{se}, \code{lower}, \code{upper}.}
\item{\code{surface}}{For \code{vary = "joint"}: a data frame with columns
\code{T1}, \code{T2}, \code{mu_hat}.  \code{NULL} for marginal mode.}
\item{\code{vary}}{The \code{vary} argument used.}
\item{\code{trt_names}}{Character vector of treatment column names.}
\item{\code{model_type}}{Outcome model type from the ATE object.}
}
}
\description{
Evaluates the GPS-weighted G-computation estimator
\eqn{\hat\mu(\mathbf{t}) = n^{-1}\sum_i \hat g(\mathbf{t}, \mathbf{X}_i)}
over a grid of treatment values using the fitted outcome model stored in
an \code{"gps_ate"} object.
}

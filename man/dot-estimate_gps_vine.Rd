% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_gps_vine.R
\name{.estimate_gps_vine}
\alias{.estimate_gps_vine}
\title{Estimate GPS via vine copula (d > 2)}
\usage{
.estimate_gps_vine(
  data,
  y,
  d,
  margins,
  params,
  vine_type,
  vine_refit_every,
  freeze_structure,
  impute_method,
  parallel_margins,
  stabilization,
  trim_quantiles,
  trim_method,
  mahal_quantile,
  df,
  lod,
  n_impute_iter,
  conv_tol,
  verbose
)
}
\arguments{
\item{data}{List from \code{\link[=prepare_data]{prepare_data()}}, or a hand-built list with \verb{$y}
(n × d treatment matrix) and \verb{$x} (list of d design matrices).}

\item{y}{n × d treatment matrix (already coerced to matrix)}

\item{d}{number of treatments (>= 3)}

\item{margins}{Character vector of length d specifying marginal
distributions.  Options: \code{"normal"}, \code{"lognormal"}, \code{"t"}, \code{"gamma"},
\code{"invgauss"}.}

\item{params}{Optional list with pre-specified \verb{$mean} and \verb{$sd}.}

\item{vine_type}{d > 2 only. \code{"auto"} (R-vine, default), \code{"C"} (C-vine),
or \code{"D"} (D-vine).}

\item{vine_refit_every}{d > 2. Refit the vine every k imputation iterations.
\code{1L} (default) = every iteration.  \code{Inf} = first iteration only
(equivalent to \code{freeze_structure = TRUE}).}

\item{freeze_structure}{d > 2. When \code{TRUE}, freezes the vine tree structure
after the first fit; subsequent iterations re-estimate pair copula
parameters only.  Default: \code{FALSE}.}

\item{impute_method}{d > 2. \code{"gaussian_approx"} (default, fast) or
\code{"vine_exact"} (slower but exact conditional imputation).}

\item{parallel_margins}{d > 2. Parallelise marginal fitting via
\code{future_lapply}.  \code{NULL} = auto (enabled for d >= 4).}

\item{stabilization}{Weight stabilisation: \code{"none"}, \code{"std"}, \code{"trim"},
or \code{"both"}.  Default: \code{"std"}.}

\item{trim_quantiles}{Quantile bounds for weight winsorization.
Default: \code{c(0.01, 0.99)}.}

\item{trim_method}{Overlap trimming method applied to treatments.
\code{"quantile"} (default, weight-space only), \code{"hull"} (convex hull, d = 2),
\code{"mahalanobis"} (chi-sq distance, any d).}

\item{mahal_quantile}{Chi-squared quantile for Mahalanobis trimming.
Default: \code{0.975}.}

\item{df}{Degrees of freedom for Student-t marginals (\code{NULL} = estimate).}

\item{lod}{Optional LOD thresholds (length d, or scalar recycled).}

\item{n_impute_iter}{Maximum imputation iterations.  Default: \code{30L}.}

\item{conv_tol}{Convergence tolerance for imputation.  Default: \code{1e-4}.}

\item{verbose}{Print informational messages.  Default: \code{TRUE}.}
}
\value{
Object of class \code{"gps_weights"}
}
\description{
Called automatically by \code{\link[=estimate_gps]{estimate_gps()}} when d > 2.  Fits marginal models,
runs the iterative vine imputation loop (if LOD censoring is present), then
fits a vine copula for both the empirical and parametric uniforms to compute
GPS weights in log space.
}
\keyword{internal}

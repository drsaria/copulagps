% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ate.R
\name{get_augmented_data}
\alias{get_augmented_data}
\title{Extract the augmented (imputed) dataset from a GPS object}
\usage{
get_augmented_data(gps, outcome = NULL)
}
\arguments{
\item{gps}{Object of class \code{"gps_weights"}}

\item{outcome}{Optional numeric outcome vector of length n.  When supplied
it is attached as column \code{"outcome"}.}
}
\value{
A \code{data.frame} with columns \code{T1}, \code{T2}, \code{w}, and optionally
\code{outcome}, with one row per observation.  Rows for hull-trimmed
observations have \code{w = NA}.
}
\description{
Returns a tidy data frame containing the (possibly imputed) treatment
values, GPS weights, and optionally the outcome.  This is the entry point
for applying user-specified models to the augmented data.
}
